# Core
beautifulsoup4>=4.12
lxml>=5.0
PyYAML>=6.0

# HTTP fetcher with browser TLS fingerprint impersonation.
# This is what makes eBay actually answer; plain requests/urllib get 403.
curl_cffi>=0.7

# Needed for `--engine playwright`, which drives a real Chromium. This is the
# route that works when eBay refuses plain HTTP clients. After installing:
#   playwright install chromium
playwright>=1.44
